{
  "name": "qrc-mock",
  "version": "1.0.0",
  "private": "true",
  "description": "Responds to websocket events sort of similarly to QRC",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "start": "node ./dist/index.js",
    "build": "npm run clean && npm i && tsc",
    "clean": "rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.3",
    "@q-sys/qrwc": "file:../../"
  },
  "dependencies": {
    "ws": "^8.18.2"
  }
}
